<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Админка — Cheboko Coffee Cup</title>
  <style>
    body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:#fff}
    .bar{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;border-bottom:1px solid rgba(0,0,0,.08)}
    .hint{padding:10px 16px;color:rgba(0,0,0,.65);border-bottom:1px dashed rgba(0,0,0,.12)}
    .hint code{background:rgba(0,0,0,.06);padding:2px 6px;border-radius:8px}
  </style>
  <!-- Decap CMS (бывш. Netlify CMS). Для production нужен backend (Git Gateway / OAuth). -->
  <script src="https://unpkg.com/decap-cms@^3.4.0/dist/decap-cms.js"></script>
</head>
<body>
  <div class="bar">
    <strong>Cheboko Coffee Cup — Админка</strong>
    <a href="../index.html">Открыть сайт</a>
  </div>
  <div class="hint">
    Это <b>макет админ‑интерфейса</b>. Для работы редактирования на GitHub Pages нужен backend авторизации.
    Для локального теста используйте <code>npx serve</code> и включите <code>local_backend: true</code> в <code>config.yml</code>.
  </div>
</body>
</html>
